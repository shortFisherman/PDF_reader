# Tasks

- [x] 1. 创建 `translation_settings.py`，移入 `build_settings()` 及依赖
- [x] 2. 精简 `pdf_renderer.py`，删除 `build_settings()` 及多余 import
- [x] 3. 更新 `routes.py` 和 `tests/test_services.py` 的 import 路径，跑测试确认
